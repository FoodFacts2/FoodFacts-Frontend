export default class User {
    constructor({ id, name, email, preferences }) {
        this.id = id;
        this.name = name;
        this.email = email;
        this.preferences = preferences;
    }
}